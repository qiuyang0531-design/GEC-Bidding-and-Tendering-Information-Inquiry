# 日期字段说明文档

## 📋 概述

在绿色电力证书交易中，日期信息对于了解项目进度至关重要。本应用将原来的单一"交易日期"字段拆分为三个独立的日期字段，以更准确地反映招标和中标的完整时间线。

## 🎯 三个日期字段

### 1. 招标开始日期（bid_start_date）
- **含义**：招标公告发布或开始接受投标的日期
- **数据库字段**：`bid_start_date DATE`
- **显示格式**：YYYY/M/D（如"2025/11/1"）
- **可为空**：是

### 2. 招标结束日期（bid_end_date）
- **含义**：招标截止日期，投标的最后期限
- **数据库字段**：`bid_end_date DATE`
- **显示格式**：YYYY/M/D（如"2025/11/15"）
- **可为空**：是

### 3. 中标日期（award_date）
- **含义**：公布中标结果的日期
- **数据库字段**：`award_date DATE`
- **显示格式**：YYYY/M/D（如"2025/11/20"）或"-"（招标中）
- **可为空**：是

## 💡 为什么需要三个日期字段？

### 原来的问题
- 单一的"交易日期"字段无法准确表示招标和中标的完整流程
- 无法区分招标信息和中标信息
- 丢失了招标时间线的重要信息

### 现在的优势
1. **完整的时间线**：清晰展示从招标开始到中标的完整过程
2. **准确的信息分类**：区分招标信息和中标信息
3. **灵活的查询**：可以按不同日期维度筛选数据
4. **真实的业务反映**：如实记录招标和中标的实际情况

## 🗄️ 数据库设计

### 字段定义
```sql
bid_start_date DATE,    -- 招标开始日期
bid_end_date DATE,      -- 招标结束日期
award_date DATE         -- 中标日期
```

### 特点
- 类型：DATE
- 可空：是（所有三个字段都允许NULL）
- 默认值：NULL

### 字段注释
```sql
COMMENT ON COLUMN transactions.bid_start_date IS '招标开始日期';
COMMENT ON COLUMN transactions.bid_end_date IS '招标结束日期';
COMMENT ON COLUMN transactions.award_date IS '中标日期';
```

## 🎨 UI显示

### TransactionTable组件实现

#### 表头
```typescript
<TableHead>招标开始日期</TableHead>
<TableHead>招标结束日期</TableHead>
<TableHead>中标日期</TableHead>
```

#### 数据行
```typescript
<TableCell>{formatDate(transaction.bid_start_date)}</TableCell>
<TableCell>{formatDate(transaction.bid_end_date)}</TableCell>
<TableCell>{formatDate(transaction.award_date)}</TableCell>
```

#### formatDate函数
```typescript
const formatDate = (date?: string) => {
  if (!date) return '-';
  return new Date(date).toLocaleDateString('zh-CN');
};
```

### 显示效果

| 字段 | 有值 | 无值 |
|------|------|------|
| 招标开始日期 | 2025/11/1 | - |
| 招标结束日期 | 2025/11/15 | - |
| 中标日期 | 2025/11/20 | - |

## 🔧 数据抓取处理

### 解析函数

```typescript
// 提取日期（支持多种格式）
function parseDate(text: string): string | null {
  const cleaned = cleanText(text);
  
  // YYYY-MM-DD
  let match = cleaned.match(/(\d{4})-(\d{2})-(\d{2})/);
  if (match) return match[0];
  
  // YYYY年MM月DD日
  match = cleaned.match(/(\d{4})年(\d{1,2})月(\d{1,2})日/);
  if (match) {
    const [, year, month, day] = match;
    return `${year}-${month.padStart(2, '0')}-${day.padStart(2, '0')}`;
  }
  
  // YYYY/MM/DD
  match = cleaned.match(/(\d{4})\/(\d{1,2})\/(\d{1,2})/);
  if (match) {
    const [, year, month, day] = match;
    return `${year}-${month.padStart(2, '0')}-${day.padStart(2, '0')}`;
  }
  
  return null;
}
```

### 使用示例

```typescript
const transaction = {
  // ... 其他字段
  bid_start_date: parseDate('2025年11月1日'),      // 返回 "2025-11-01"
  bid_end_date: parseDate('2025-11-15'),           // 返回 "2025-11-15"
  award_date: parseDate('2025/11/20'),             // 返回 "2025-11-20"
};
```

## 📊 示例数据

### 已中标项目示例

#### 示例1：风电项目
```
项目名称：2025年度风电绿色电力证书交易项目
招标开始日期：2025-11-01
招标结束日期：2025-11-15
中标日期：2025-11-20
状态：✅ 已中标
说明：完整的招标和中标时间线
```

#### 示例2：光伏项目（跨年度）
```
项目名称：光伏绿证采购项目（第一批）
招标开始日期：2024-12-10
招标结束日期：2024-12-25
中标日期：2025-01-05
状态：✅ 已中标
说明：招标在2024年底，中标在2025年初
```

### 招标中项目示例

#### 示例3：集中式风电
```
项目名称：集中式风电绿证交易（第二批）
招标开始日期：2025-12-15
招标结束日期：2025-12-30
中标日期：-
状态：⏳ 招标中
说明：项目正在招标中，尚未公布中标结果
```

## 🔍 查询和筛选

### SQL查询示例

```sql
-- 查询所有已中标的项目
SELECT * FROM transactions WHERE award_date IS NOT NULL;

-- 查询所有招标中的项目
SELECT * FROM transactions WHERE award_date IS NULL;

-- 查询指定日期范围内开始招标的项目
SELECT * FROM transactions 
WHERE bid_start_date BETWEEN '2025-11-01' AND '2025-12-31';

-- 查询指定日期范围内中标的项目
SELECT * FROM transactions 
WHERE award_date BETWEEN '2025-11-01' AND '2025-12-31';

-- 查询招标周期（招标开始到结束的天数）
SELECT 
  project_name,
  bid_start_date,
  bid_end_date,
  bid_end_date - bid_start_date as 招标天数
FROM transactions
WHERE bid_start_date IS NOT NULL AND bid_end_date IS NOT NULL;

-- 查询从招标结束到中标的天数
SELECT 
  project_name,
  bid_end_date,
  award_date,
  award_date - bid_end_date as 评标天数
FROM transactions
WHERE bid_end_date IS NOT NULL AND award_date IS NOT NULL;
```

## ⚠️ 注意事项

### 1. 数据完整性
- 所有三个日期字段都允许为NULL
- 这不是数据缺失，而是业务上的实际情况
- 招标中的项目：award_date为NULL
- 只有中标信息的数据：bid_start_date和bid_end_date可能为NULL

### 2. 数据抓取
- 根据实际网站内容提取相应的日期字段
- 如果网站只提供中标日期，其他字段设为NULL
- 如果网站只提供招标日期，award_date设为NULL
- 使用parseDate函数统一处理各种日期格式

### 3. UI显示
- NULL值显示为"-"，而不是空白
- 这样用户可以明确知道该字段存在但未填写
- 日期格式统一为"YYYY/M/D"（中文本地化格式）

### 4. 数据分析
- 在统计分析时，需要考虑NULL值
- 可以单独统计招标中的项目（award_date IS NULL）
- 可以分析招标周期和评标周期

## 📈 实际应用场景

### 场景1：完整信息的已中标项目
```
项目名称：2025年度风电绿证交易
招标开始：2025-11-01 ✅
招标结束：2025-11-15 ✅
中标日期：2025-11-20 ✅
说明：所有日期信息完整，用户可以全面了解项目时间线
```

### 场景2：招标中的项目
```
项目名称：集中式风电绿证交易（第二批）
招标开始：2025-12-15 ✅
招标结束：2025-12-30 ✅
中标日期：- ⏳
说明：项目正在招标中，中标日期尚未确定
```

### 场景3：只有中标信息的项目
```
项目名称：某历史项目
招标开始：- 
招标结束：- 
中标日期：2025-10-15 ✅
说明：只抓取到中标信息，招标日期未知
```

### 场景4：跨年度招标
```
项目名称：光伏绿证采购项目
招标开始：2024-12-10 ✅
招标结束：2024-12-25 ✅
中标日期：2025-01-05 ✅
说明：招标在2024年底，中标在2025年初，清晰展示跨年度流程
```

## 🎓 最佳实践

### 1. 数据抓取时
```typescript
// ✅ 正确：根据实际情况填写
const transaction = {
  bid_start_date: parseDate(extractedBidStart) || null,
  bid_end_date: parseDate(extractedBidEnd) || null,
  award_date: parseDate(extractedAward) || null,
};

// ❌ 错误：强制填充不存在的数据
const transaction = {
  bid_start_date: parseDate(extractedBidStart) || '2025-01-01', // 不要用默认值
  bid_end_date: parseDate(extractedBidEnd) || '2025-01-01',
  award_date: parseDate(extractedAward) || '2025-01-01',
};
```

### 2. 数据验证时
```typescript
// ✅ 正确：允许NULL值
if (transaction.bid_start_date && transaction.bid_end_date) {
  // 验证招标结束日期晚于开始日期
  if (new Date(transaction.bid_end_date) < new Date(transaction.bid_start_date)) {
    console.warn('招标结束日期早于开始日期');
  }
}

// ❌ 错误：将NULL视为错误
if (!transaction.bid_start_date) {
  throw new Error('招标开始日期不能为空');
}
```

### 3. UI显示时
```typescript
// ✅ 正确：使用formatDate函数
<TableCell>{formatDate(transaction.bid_start_date)}</TableCell>

// ❌ 错误：直接显示原始值
<TableCell>{transaction.bid_start_date}</TableCell>
```

### 4. 数据查询时
```typescript
// ✅ 正确：考虑NULL值
const params: any = { user_id: userId };
if (startDate) {
  // 可以按任一日期字段筛选
  params.bid_start_date = { gte: startDate };
}

// ❌ 错误：假设所有字段都有值
const params = {
  user_id: userId,
  bid_start_date: { gte: startDate }, // 如果startDate为空会出错
};
```

## 🔄 数据迁移历史

### v1.4.0 迁移
- **迁移名称**：split_transaction_date_to_three_dates
- **操作**：
  1. 添加三个新字段：bid_start_date、bid_end_date、award_date
  2. 将现有transaction_date数据迁移到award_date
  3. 删除transaction_date字段
  4. 添加字段注释

### 迁移SQL
```sql
-- 1. 添加新字段
ALTER TABLE transactions 
ADD COLUMN bid_start_date DATE,
ADD COLUMN bid_end_date DATE,
ADD COLUMN award_date DATE;

-- 2. 迁移数据
UPDATE transactions 
SET award_date = transaction_date 
WHERE transaction_date IS NOT NULL;

-- 3. 删除旧字段
ALTER TABLE transactions 
DROP COLUMN transaction_date;

-- 4. 添加注释
COMMENT ON COLUMN transactions.bid_start_date IS '招标开始日期';
COMMENT ON COLUMN transactions.bid_end_date IS '招标结束日期';
COMMENT ON COLUMN transactions.award_date IS '中标日期';
```

## 📚 相关文档

- [数据抓取定制指南](SCRAPING_GUIDE.md) - 详细的parseDate实现
- [快速开始指南](QUICKSTART.md) - 包含完整日期信息的示例数据
- [更新日志](CHANGELOG.md) - v1.4.0版本更新详情

## 🔄 版本历史

### v1.4.0 (2026-01-26)
- ✅ 新增：日期字段拆分为三个独立字段
- ✅ 更新：示例数据包含完整日期信息
- ✅ 优化：UI表格显示三个日期列
- ✅ 文档：完善日期字段说明

### v1.3.0 (2026-01-26)
- ✅ 绿证年份支持多年份格式

### v1.2.0 (2026-01-26)
- ✅ 通道类型支持三种状态

### v1.1.0 (2026-01-26)
- ✅ 价格数据修正

### v1.0.0 (2026-01-26)
- ✅ 初始版本：单一transaction_date字段

---

**最后更新**：2026-01-26
**版本**：v1.4.0
